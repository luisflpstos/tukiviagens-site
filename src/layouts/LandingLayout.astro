---
import BaseLayout from './BaseLayout.astro';
import WhatsAppButton from '../components/common/WhatsAppButton.astro';

interface Props {
	title: string;
	description: string;
	path: string;
	noindex?: boolean;
	whatsapp: string;
	campaign?: string;
	whatsappProduct?: string;
}

const { title, description, path, noindex = true, whatsapp, campaign, whatsappProduct } = Astro.props;
---

<BaseLayout
	title={title}
	description={description}
	path={path}
	noindex={noindex}
	showHeader={false}
	showFooter={false}
>
	<slot />
	<WhatsAppButton
		phone={whatsapp}
		product={whatsappProduct ?? title}
		campaign={campaign}
		h1={title}
		fixed
		label=""
		showLabel={false}
		class="!px-4"
	/>
</BaseLayout>
