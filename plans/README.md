# Implementation Plans

Generated by the improve skill on 2026-07-16. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Operator constraints for this session: use **pnpm**; do **not** create branches/worktrees; do **not** commit or push unless explicitly asked.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Dock lead form under sticky carousel on desktop until highlights release (enter/exit motion required) | P1 | M | — | DONE |
| 002  | Fit docked sticky lead form fully on short desktop viewports (density + carousel budget; mobile untouched) | P1 | S | 001 (DONE) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002 assumes the dock teleport from 001 is already shipping (`is-sticky-lead-docked`, `[data-sticky-lead-*]` hooks). It only changes docked density / height budget.

## Findings considered and rejected

- **Clone form into sticky column**: rejected — would duplicate `formId`/DOM ids and break LeadForm init; plan uses single-node teleport instead.
- **Always keep form inside sticky column in Astro markup**: rejected — user requires the form to return to its original pre-footer position when destaques enter; runtime dock/undock matches that contract.
- **Apply behavior on LP / DestinationLayout**: rejected for this plan — those layouts have no `ContentWithCarousel` sticky gallery.
- **Only fix `max-height: calc(100vh - 9rem)` without shrinking the form**: rejected — measured at 1100×750 the panel starts below the carousel (~543px top) with only ~207px visible; raising max-height alone keeps a useless peek-scroll. Density + carousel budget required.
- **Shrink / restyle mobile `LeadCaptureSection`**: rejected — user explicitly forbids mobile changes; dock never activates below 1024px.
