# Implementation Plans

Generated by the improve skill on 2026-07-16 (`plan` variant — user-requested sticky-carousel lead dock). Execute in the order below. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Operator constraints for this session: use **pnpm**; do **not** create branches/worktrees; do **not** commit or push unless explicitly asked.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Dock lead form under sticky carousel on desktop until highlights release (enter/exit motion required) | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Single plan; no ordering dependencies.

## Findings considered and rejected

- **Clone form into sticky column**: rejected — would duplicate `formId`/DOM ids and break LeadForm init; plan uses single-node teleport instead.
- **Always keep form inside sticky column in Astro markup**: rejected — user requires the form to return to its original pre-footer position when destaques enter; runtime dock/undock matches that contract.
- **Apply behavior on LP / DestinationLayout**: rejected for this plan — those layouts have no `ContentWithCarousel` sticky gallery.
